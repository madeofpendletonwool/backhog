# Build output
web/dist/
web/node_modules/
api/backhog

# Vendored OCR runtime — 14 MB of Tesseract WASM and language data copied out
# of node_modules by web/scripts/vendor-ocr.mjs on every build. npm pins the
# versions in the lockfile; the bytes do not belong in git.
web/public/assets/ocr/

# Local data
*.db
*.db-shm
*.db-wal
covers/
data/

# Environment
.env

# Editor / OS
.DS_Store
.idea/
.vscode/

# upstream art packs — see scripts/build-assets.py. What ships is the derived
# output in web/public/assets, not the packs themselves.
/icon-packs/

# python bytecode from the asset scripts
__pycache__/
